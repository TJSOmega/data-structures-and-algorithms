// CLASS EXAMPLE

class HashMap {
  constructor(size) {
    this.size = size;
    this.map = new Array.size(size).fill()
  }

  hash(key) {
    return 4;
  }

  set(key, value) {

  }
  // Returns a value in the Hash Map
  get(key) {

  }
  // Returns a boolean regarding if the object is in the Hash Map
  has(key) {

  }
}